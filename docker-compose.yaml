
version: '3'

services:
  app:
    build: './swaggereditor'
    ports:
      - '2200:8080'
  api:
    build: './api'
    volumes:
      - './api/html:/usr/share/nginx/html/'
    ports:
      - '3300:80'

